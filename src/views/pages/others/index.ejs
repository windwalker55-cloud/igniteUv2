<style>
  /* ===== BANNER — IgniteU Theme ===== */
  .welcome-card {
    position: relative;
    background: linear-gradient(160deg, rgba(94,234,212,.09), rgba(6,32,50,.7));
    border: 1px solid var(--line);
    backdrop-filter: blur(14px);
    -webkit-backdrop-filter: blur(14px);
    border-radius: 16px;
    padding: 1.1rem 1.4rem;
    margin: 10px 10px 12px 10px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    overflow: hidden;
    min-height: 90px;
  }
  .welcome-card::before {
    content: "";
    position: absolute; top: 0; left: 0; right: 0; height: 1px;
    background: linear-gradient(90deg, transparent, var(--mint), transparent);
    opacity: .45;
  }
  .welcome-content { flex: 1; }
  .welcome-content h2 {
    font: 700 1.25rem 'Inter', sans-serif;
    color: var(--text); margin-bottom: .25rem; letter-spacing: -.01em;
  }
  .welcome-content h2 .greeting { color: var(--mint); }
  .welcome-content h2 .username { color: #fff; }
  .welcome-content p {
    font-size: .88rem; color: var(--muted); margin: 0;
    font-family: 'JetBrains Mono', monospace;
  }
  .banner-right { display: flex; align-items: center; gap: 1.1rem; flex-shrink: 0; }
  .datetime-section { text-align: right; }
  .date { font: 500 .8rem 'JetBrains Mono', monospace; color: var(--muted); letter-spacing: .5px; }
  .time { font: 700 1.15rem 'Inter', sans-serif; color: var(--text); letter-spacing: -.01em; }

  /* ===== QUICK ACCESS — IgniteU Theme ===== */
  .quick-access {
    position: relative;
    background: linear-gradient(160deg, rgba(94,234,212,.06), rgba(2,16,30,.6));
    border: 1px solid var(--line);
    border-radius: 16px;
    padding: 1.3rem 1.3rem;
    margin: 0 10px 18px 10px;
    overflow: hidden;
  }
  .quick-access::before {
    content: "";
    position: absolute;
    top: 0; left: 0; right: 0; height: 1px;
    background: linear-gradient(90deg, transparent, var(--mint), transparent);
    opacity: .4;
  }
  .section-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 1.1rem;
    flex-wrap: wrap;
    gap: .5rem;
  }
  .section-header h2 {
    font: 700 1rem 'Inter', sans-serif;
    color: var(--text);
    letter-spacing: -.01em;
  }
  .quick-access-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 0.85rem;
  }
  .access-card {
    position: relative;
    background: rgba(94,234,212,.04);
    border: 1px solid var(--line);
    border-radius: 14px;
    padding: 1rem;
    cursor: pointer;
    transition: background .25s, border-color .25s, transform .25s, box-shadow .25s;
    display: flex;
    flex-direction: column;
    gap: .1rem;
    overflow: hidden;
  }
  .access-card::after {
    content: "";
    position: absolute; inset: 0;
    border-radius: inherit;
    background: radial-gradient(circle at top left, rgba(94,234,212,.06), transparent 70%);
    opacity: 0; transition: opacity .3s; pointer-events: none;
  }
  .access-card:hover { background: rgba(94,234,212,.09); border-color: var(--line-strong); transform: translateY(-3px); box-shadow: 0 12px 32px rgba(94,234,212,.12); }
  .access-card:hover::after { opacity: 1; }
  .access-icon {
    width: 42px; height: 42px; border-radius: 10px;
    display: flex; align-items: center; justify-content: center;
    font-size: 1.3rem; margin-bottom: 0.7rem; flex-shrink: 0;
  }
  .access-content h3 { font: 600 0.9rem 'Inter', sans-serif; color: var(--text); margin-bottom: 0.25rem; letter-spacing: -.01em; }
  .access-content p  { font-size: 0.75rem; color: var(--muted); line-height: 1.5; margin-bottom: 0.6rem; }
  .access-action { margin-top: auto; }
  .access-btn {
    width: 100%; padding: 0.5rem 0.75rem; border-radius: 999px; border: none; cursor: pointer;
    background: linear-gradient(135deg, var(--mint-bright), var(--mint-deep));
    color: #022; font: 600 0.78rem 'Inter', sans-serif; letter-spacing: -.01em;
    box-shadow: 0 4px 16px rgba(94,234,212,.3); transition: transform .2s, box-shadow .2s;
  }
  .access-btn:hover { transform: translateY(-1px); box-shadow: 0 6px 22px rgba(94,234,212,.45); }
  .access-btn:active { transform: translateY(0); }

  /* ===== EDU STRIP ===== */
  .edu-strip {
    margin: 0 10px 18px;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: .7rem;
  }
  .edu-card {
    position: relative;
    background: rgba(94,234,212,.04);
    border: 1px solid var(--line);
    border-radius: 14px;
    padding: .85rem 1rem .8rem;
    overflow: hidden;
    transition: background .2s, border-color .2s;
    display: flex; flex-direction: column;
  }
  .edu-card:hover { background: rgba(94,234,212,.07); border-color: var(--line-strong); }
  .edu-card::before {
    content: ""; position: absolute; top: 0; left: 0; right: 0; height: 1px;
    background: linear-gradient(90deg, transparent, var(--mint), transparent); opacity: .3;
  }
  .edu-card-header { display: flex; align-items: center; justify-content: space-between; margin-bottom: .45rem; }
  .edu-card-label {
    font: 600 .62rem 'JetBrains Mono', monospace;
    color: var(--mint); letter-spacing: 1.5px; text-transform: uppercase;
    display: flex; align-items: center; gap: .35rem;
  }
  .edu-arrows { display: flex; gap: .3rem; }
  .edu-arrow {
    width: 24px; height: 24px; border-radius: 50%;
    border: 1px solid var(--line); background: rgba(94,234,212,.07);
    color: var(--mint); display: flex; align-items: center; justify-content: center;
    cursor: pointer; font-size: .6rem;
    transition: background .2s, border-color .2s, transform .15s;
  }
  .edu-arrow:hover { background: rgba(94,234,212,.16); border-color: var(--line-strong); transform: scale(1.1); }
  .edu-arrow:active { transform: scale(.92); }
  .edu-card-text   { font: 400 .88rem 'Inter', sans-serif; color: var(--text); line-height: 1.6; flex: 1; transition: opacity .4s ease, transform .4s ease; }
  .edu-card-source { font: 400 .68rem 'JetBrains Mono', monospace; color: var(--muted-2); margin-top: .25rem; min-height: .85rem; transition: opacity .4s ease; }
  .edu-fading { opacity: 0; transform: translateY(6px); }
  .edu-card-footer { display: flex; align-items: center; justify-content: space-between; margin-top: .55rem; }
  .edu-dots { display: flex; gap: .32rem; flex-wrap: wrap; }
  .edu-dot { width: 5px; height: 5px; border-radius: 50%; background: var(--line-strong); cursor: pointer; transition: background .2s, transform .15s; }
  .edu-dot.active { background: var(--mint); transform: scale(1.3); }
  .edu-progress { height: 2px; width: 60px; border-radius: 999px; background: var(--line); overflow: hidden; display: none; }
  .edu-progress-fill { height: 100%; width: 0%; background: var(--mint); border-radius: 999px; }

  /* ===== RESPONSIVE ===== */
  @media (max-width: 768px) {
    .welcome-card       { padding: 1rem 1.2rem; min-height: 80px; }
    .welcome-content h2 { font-size: 1.15rem; }
    .welcome-content p  { font-size: .88rem; }
    .time               { font-size: 1.05rem; }
    .date               { font-size: .78rem; }
    .quick-access       { margin: 0 8px 16px 8px; padding: 1.4rem 1.3rem; }
    .section-header h2  { font-size: 1.2rem; }
    .access-icon        { width: 50px; height: 50px; font-size: 1.5rem; }
    .access-content h3  { font-size: 1.05rem; }
    .access-content p   { font-size: .88rem; }
    .access-btn         { font-size: .9rem; padding: 0.6rem; }
    .edu-strip          { margin: 0 8px 16px; }
    .edu-card-label     { font-size: .7rem; }
    .edu-card-text      { font-size: .92rem; }
  }
  @media (max-width: 576px) {
    .welcome-card       { padding: .9rem 1rem; min-height: 75px; gap: .75rem; }
    .welcome-content h2 { font-size: 1.05rem; }
    .welcome-content p  { font-size: .85rem; }
    .time               { font-size: .95rem; }
    .quick-access-grid  { grid-template-columns: 1fr; }
    .access-card        { flex-direction: row; align-items: center; gap: 0.9rem; padding: 0.95rem 1rem; }
    .access-icon        { margin-bottom: 0; width: 48px; height: 48px; font-size: 1.4rem; }
    .access-content     { flex: 1; }
    .access-content p   { margin-bottom: 0; font-size: .85rem; }
    .access-content h3  { font-size: 1rem; }
    .access-action      { flex-shrink: 0; }
    .access-btn         { width: auto; padding: 0.55rem 1.2rem; white-space: nowrap; font-size: .88rem; }
    .edu-strip          { grid-template-columns: 1fr; }
    .edu-card-text      { font-size: .9rem; }
  }
  @media (max-width: 380px) {
    .welcome-card       { padding: .85rem .9rem; }
    .welcome-content h2 { font-size: .95rem; }
    .date               { display: none; }
    .quick-access       { padding: 1rem; }
    .access-icon        { width: 44px; height: 44px; font-size: 1.25rem; }
    .access-content h3  { font-size: .95rem; }
    .access-content p   { font-size: .82rem; }
    .access-btn         { font-size: .84rem; padding: 0.5rem 1rem; }
  }
</style>

<!-- Banner -->
<div class="welcome-card">
  <div class="welcome-content">
    <h2>
      <span class="greeting" id="good">Welcome!</span>
      <% if (user) { %>
        <span class="username"> <%= user.username %></span>
      <% } %>
    </h2>
    <p>Ready for your next session?</p>
  </div>
  <div class="banner-right">
    <div class="datetime-section">
      <div class="date" id="date"></div>
      <div class="time" id="time"></div>
    </div>
  </div>
</div>

<!-- Education Strip -->
<div class="edu-strip">

  <!-- Study Tips carousel -->
  <div class="edu-card">
    <div class="edu-card-header">
      <div class="edu-card-label"><i class="fa-solid fa-lightbulb"></i> Study Tip</div>
      <div class="edu-arrows">
        <button class="edu-arrow" id="tipPrev"><i class="fa-solid fa-chevron-left"></i></button>
        <button class="edu-arrow" id="tipNext"><i class="fa-solid fa-chevron-right"></i></button>
      </div>
    </div>
    <div class="edu-card-text"   id="tipText"></div>
    <div class="edu-card-source" id="tipSource"></div>
    <div class="edu-card-footer">
      <div class="edu-dots" id="tipDots"></div>
      <div class="edu-progress"><div class="edu-progress-fill" id="tipFill"></div></div>
    </div>
  </div>

  <!-- Did You Know carousel -->
  <div class="edu-card">
    <div class="edu-card-header">
      <div class="edu-card-label"><i class="fa-solid fa-flask"></i> Did You Know?</div>
      <div class="edu-arrows">
        <button class="edu-arrow" id="factPrev"><i class="fa-solid fa-chevron-left"></i></button>
        <button class="edu-arrow" id="factNext"><i class="fa-solid fa-chevron-right"></i></button>
      </div>
    </div>
    <div class="edu-card-text"   id="factText"></div>
    <div class="edu-card-source" id="factSource"></div>
    <div class="edu-card-footer">
      <div class="edu-dots" id="factDots"></div>
      <div class="edu-progress"><div class="edu-progress-fill" id="factFill"></div></div>
    </div>
  </div>

</div>

<!-- Quick Access -->
<div class="quick-access">
  <div class="section-header">
    <h2>Quick Access</h2>
  </div>
  <div class="quick-access-grid">

    <div class="access-card" onclick="window.location.href='/quickquiz'">
      <div class="access-icon" style="background: linear-gradient(135deg, #3b82f6, #1d4ed8);">🧠</div>
      <div class="access-content">
        <h3>Pratice Mode</h3>
        <p>Test your knowledge with Pratice Mode</p>
      </div>
      <div class="access-action"><button class="access-btn">Start</button></div>
    </div>

    <div class="access-card" onclick="window.location.href='/mocktest'">
      <div class="access-icon" style="background: linear-gradient(135deg, #10b981, #059669);">🎯</div>
      <div class="access-content">
        <h3>Test Mode</h3>
        <p>Practice under real test conditions</p>
      </div>
      <div class="access-action"><button class="access-btn">Start</button></div>
    </div>

    <div class="access-card" onclick="window.location.href='/mockexam'">
      <div class="access-icon" style="background: linear-gradient(135deg, #f59e0b, #d97706);">📝</div>
      <div class="access-content">
        <h3>Exam Mode</h3>
        <p>Full-length exams to simulate real experience</p>
      </div>
      <div class="access-action"><button class="access-btn">Start</button></div>
    </div>

    <div class="access-card" onclick="window.location.href='/ai'">
      <div class="access-icon" style="background: linear-gradient(135deg, #8b5cf6, #7c3aed);">🤖</div>
      <div class="access-content">
        <h3>AI Assistant</h3>
        <p>Get help, explanations & study guidance instantly</p>
      </div>
      <div class="access-action"><button class="access-btn">Open</button></div>
    </div>

    <div class="access-card" onclick="window.location.href='/leaderboard'">
      <div class="access-icon" style="background: linear-gradient(135deg, #ec4899, #db2777);">🏆</div>
      <div class="access-content">
        <h3>Leaderboard</h3>
        <p>See rankings and compare your performance</p>
      </div>
      <div class="access-action"><button class="access-btn">View</button></div>
    </div>

    <div class="access-card" onclick="window.location.href='/materials'">
      <div class="access-icon" style="background: linear-gradient(135deg, #06b6d4, #0891b2);">📚</div>
      <div class="access-content">
        <h3>Study Material</h3>
        <p>Access notes, resources & course materials</p>
      </div>
      <div class="access-action"><button class="access-btn">Open</button></div>
    </div>

  </div>
</div>

<script>
(function () {
  /* ── Clock & greeting ── */
  var greetEl = document.getElementById('good');
  if (greetEl) {
    var h = new Date().getHours();
    greetEl.textContent = h < 12 ? 'Good Morning! ☀️' : h < 18 ? 'Good Afternoon! 🌤️' : 'Good Evening! 🌙';
  }
  function updateClock() {
    var now = new Date();
    var d = document.getElementById('date');
    var t = document.getElementById('time');
    if (d) d.textContent = now.toLocaleDateString('en-GB', { weekday: 'short', day: 'numeric', month: 'short' });
    if (t) t.textContent = now.toLocaleTimeString('en-GB', { hour: '2-digit', minute: '2-digit', second: '2-digit' });
  }
  updateClock();
  setInterval(updateClock, 1000);

  /* ── Edu carousels ── */
  if (window.__eduReady) return;
  window.__eduReady = true;

  var FADE_MS = 400;

  function makeCarousel(opts) {
    var items    = opts.items;
    var INTERVAL = opts.interval || 7000;
    var textEl   = document.getElementById(opts.textId);
    var srcEl    = document.getElementById(opts.sourceId);
    var dotsEl   = document.getElementById(opts.dotsId);
    var prevBtn  = document.getElementById(opts.prevId);
    var nextBtn  = document.getElementById(opts.nextId);

    var today = new Date();
    var idx   = (today.getDate() + today.getMonth() + (opts.offset || 0)) % items.length;
    var timer;

    function buildDots() {
      if (!dotsEl) return;
      dotsEl.innerHTML = items.map(function (_, i) {
        return '<div class="edu-dot' + (i === idx ? ' active' : '') + '" data-i="' + i + '"></div>';
      }).join('');
      dotsEl.querySelectorAll('.edu-dot').forEach(function (dot) {
        dot.addEventListener('click', function () { goTo(parseInt(dot.dataset.i)); });
      });
    }

    function updateDots() {
      if (!dotsEl) return;
      dotsEl.querySelectorAll('.edu-dot').forEach(function (d, i) {
        d.classList.toggle('active', i === idx);
      });
    }

    function show(animate) {
      var item = items[idx];
      if (!animate) {
        if (textEl) textEl.textContent = item.text;
        if (srcEl)  srcEl.textContent  = item.source ? '— ' + item.source : '';
        updateDots(); return;
      }
      if (textEl) textEl.classList.add('edu-fading');
      if (srcEl)  srcEl.classList.add('edu-fading');
      setTimeout(function () {
        var it = items[idx];
        if (textEl) { textEl.textContent = it.text;  textEl.classList.remove('edu-fading'); }
        if (srcEl)  { srcEl.textContent  = it.source ? '— ' + it.source : ''; srcEl.classList.remove('edu-fading'); }
        updateDots();
      }, FADE_MS);
    }

    function goTo(i) {
      idx = (i + items.length) % items.length;
      clearInterval(timer);
      show(true);
      timer = setInterval(function () { idx = (idx + 1) % items.length; show(true); }, INTERVAL);
    }

    function start() {
      buildDots();
      show(false);
      timer = setInterval(function () { idx = (idx + 1) % items.length; show(true); }, INTERVAL);
    }

    if (prevBtn) prevBtn.addEventListener('click', function () { goTo(idx - 1); });
    if (nextBtn) nextBtn.addEventListener('click', function () { goTo(idx + 1); });

    return { start: start };
  }

  makeCarousel({
    textId: 'tipText', sourceId: 'tipSource', dotsId: 'tipDots',
    fillId: 'tipFill', prevId: 'tipPrev', nextId: 'tipNext',
    interval: 8000, offset: 0,
    items: [
      { text: "Use the Pomodoro method — study for 25 minutes, then rest for 5. Consistency beats long cramming sessions.", source: "" },
      { text: "Write by hand when memorising key points. Studies show handwriting improves long-term retention.", source: "" },
      { text: "Teach what you just learned to an imaginary student. If you can explain it simply, you truly understand it.", source: "" },
      { text: "Review your notes within 24 hours of a lecture. Forgetting is fastest in the first day — catch it early.", source: "" },
      { text: "Interleave subjects during study sessions instead of blocking one topic all day. It strengthens recall.", source: "" },
      { text: "Sleep is not optional — memory consolidation happens during deep sleep. Aim for 7–9 hours before exams.", source: "" },
      { text: "Use past exam questions as your primary revision tool. Pattern recognition is faster than re-reading notes.", source: "" },
      { text: "Break large topics into small chunks and tackle one chunk per session. Progress feels real and sustainable.", source: "" }
    ]
  }).start();

  makeCarousel({
    textId: 'factText', sourceId: 'factSource', dotsId: 'factDots',
    fillId: 'factFill', prevId: 'factPrev', nextId: 'factNext',
    interval: 5500, offset: 3,
    items: [
      { text: "Spaced repetition can improve long-term memory by up to 200% compared to massed practice (cramming).", source: "Cognitive Psychology" },
      { text: "The human brain can store roughly 2.5 petabytes of information — equivalent to 3 million hours of TV.", source: "Neuroscience Research" },
      { text: "Students who take handwritten notes score higher on conceptual questions than those who type their notes.", source: "Mueller & Oppenheimer, 2014" },
      { text: "Exercise before studying increases BDNF — a protein that strengthens neural connections and aids learning.", source: "Harvard Medical School" },
      { text: "Reading aloud improves memory recall significantly — a phenomenon called the 'production effect'.", source: "University of Waterloo, 2017" },
      { text: "The Feynman Technique (explain simply → find gaps → revise) is one of the most effective learning strategies known.", source: "Richard Feynman, Nobel Laureate" },
      { text: "Albert Einstein did not speak fluently until age 9. Late development does not determine future capability.", source: "Historical Record" },
      { text: "Mathematics is the only language shared by all human cultures and is considered a universal language.", source: "Philosophy of Science" }
    ]
  }).start();

})();
</script>